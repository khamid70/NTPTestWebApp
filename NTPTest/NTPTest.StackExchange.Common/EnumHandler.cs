
namespace NTPTest.StackExchange.Common
{
    public enum OrderType
    {
        Ascending,
        Descending,
    }

    public enum SortType
    {
        Activity,
        Creation,
        Votes,
        Hot,
        Week,
        Month,
    }

    public enum RequestType
    {
        GET,
        POST,
    }
}